description: This is my driver's binding file
compatible: "zephyr,my_sensor"
include: base.yaml

properties:
  gpios:
    type: phandle-array
    required: true
    description: GPIO for driving the LED